/*var page = document.getElementsByClassName('tweet-page')[0];
var totalT = 1000;
var frameT = 20;
var startX = 0;
var endX = -480;

var startTime;
var flag;

function motionFrame(){
	if(Math.abs(easing.offset) >= 1){
		clearInterval(flag);
	}else{
		easing.motion(page, startTime, totalT, startX, endX);
	}
	
}

window.onload = function(){
	//startTime = new Date().getTime();
	//flag = setInterval('motionFrame()', frameT);
}*/

//地理信息测试
/*navigator.geolocation.getCurrentPosition(function(postion){
	alert(postion.coords.latitude);
}, function(error){
	alert(error.message);
});*/